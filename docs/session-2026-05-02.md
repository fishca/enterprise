# Session 2026-05-02 — handoff notes

Continuation of the eval / scope refactor and the bytecode-self-sufficiency
work. Smaller scope than the prior 2026-04-30 / 05-01 arc — focus on landing
Step 1 of the AOT cache, dropping the stop-gap `numberTest` project, and
filling the test gap around the compiler / runtime / bytecode core.

## What landed

### 1. AOT persistence layer (Step 1 of `docs/next-session-aot.md`) ✅

`ibByteCode::SerializeAOT(ibWriterMemory&) const` /
`DeserializeAOT(const ibReaderMemory&)` declared on the bytecode struct.
Bodies in new `src/engine/backend/compiler/byteCodeAOT.cpp` (sibling of
`valueSerialization.cpp` — different layer, hence the AOT-specific name).

Format: linear binary, host-endian, magic `'PBC1'` + format version `1`.
No `fs.h` chunk wrappers around the top-level layout — every collection
is `u32 count` + `count` entries laid out back-to-back. Strings via
`w_stringZ` / `r_stringZ` (UTF-8 zero-terminated). `TYPE_NUMBER`
constants via `ibNumber::GetBuffer` / `SetBuffer` (binary, exact-decimal —
not lossy double).

Persisted: `m_id`, `m_version`, `m_descriptorClsid`, `m_strModuleName`,
`m_lStartModule`, `m_lVarCount`, `m_bExpressionOnly`, `m_listCode`,
`m_listConst` (primitives only — `TYPE_REFFER` rejected as cache-miss),
`m_listVar`, `m_listFunc` (with embedded `m_listLocals` + `m_listParam` +
`m_listParamRealName`), `m_dependencyIds`, `m_dependencyVersions`.

Skipped (live pointers — re-resolved on load): `m_parent`,
`m_dependencies`. Reader stamps `m_bCompile = true` on success.

Sanity cap: every collection-count is rejected if > 16M.

Future Steps 2-5 (DB schema, compile-time hook, dependency registry,
invalidation tests) remain — see `docs/next-session-aot.md`.

### 2. `numberTest` project dropped

Removed from `enterprise.sln` (project def + 8 config-platform lines +
NestedProjects entry). Source dir `src/engine/test/numberTest/` deleted
(was a stop-gap MSBuild console exe duplicating
`enterprise/tests/test_number.cpp`). `CLAUDE.md` reference updated.

### 3. Test suite for compiler / runtime / bytecode

Three new files under `enterprise/tests/`, wired into `tests/CMakeLists.txt`:

- `test_byteCodeAOT.cpp` — 11 round-trip cases. Empty, header-only, all
  primitive const types incl. high-precision number, all 5 var kinds,
  function with locals + params + context-method, dependencies,
  format-magic / short-buffer / empty-buffer rejection,
  deserialize-resets-target, large-bytecode stress (5000 instr).
- `test_compiler.cpp` — 13 cases. Lexer (3) + compile-shape (10):
  assignment, var decls (incl. `Export`), function/procedure decls,
  if / while emission, syntax-error handling, plus a real-compile-output
  AOT round-trip end-to-end test.
- `test_runtime.cpp` — 14 cases. Empty bytecode no-op; constant
  assignment + GetPropVal readback; arithmetic on all 4 ops; string
  concat; comparison; if / if-else / while; function call (1 arg /
  3 args / recursion); module-state survives across multiple calls.

Sessionless runtime works because `ibSession::GetPUState()` falls back
to a `static thread_local ibProcUnitState` when no session is bound
(landed earlier in the codeRunner cleanup arc).

Tests build via `cmake -B build -DBUILD_TESTING=ON && cmake --build build
--target oes_tests`. Not yet run; some runtime cases may need adjustment
once they actually execute.

### 4. VS filter sync — `frontend.vcxproj.filters`

`frontend.vcxproj` had 28 ClCompile / ClInclude entries (codeEditor
files, components/helpers, workerPoolGUI, functionSearcher, lineInput)
that were absent from `.filters` — invisible in Solution Explorer.
Added with correct `<Filter>` values.

Folder rename `win\dlg` → `win\dlgs`: the two remaining `dlg`
subdirectories (`functionSearcher/`, `lineInput/`) coexisted on disk
alongside `dlgs/`. Moved both into `dlgs/`, removed empty `dlg/`,
updated 4 `#include` paths (2 in
`frontend/win/editor/codeEditor/codeEditor.cpp`, 2 in
`designer/docManager/templates/docViewModuleEditor.cpp`), updated
`frontend.vcxproj` + `.filters`.

Other project filters (launcher / codeRunner / classChecker / designer /
enterprise) intentionally kept their virtual organisational folders
(`windows`, `res`) — those don't correspond to disk dirs but are
deliberate Solution Explorer grouping for root-level files.

### 5. Open-items burndown

Followed up on four items that were on the previous session's
`Open items — next session` list — small, standalone, all in the
same working-tree cycle as the AOT layer.

**`ibEvalModeScope` RAII guard.** `ibProcUnit::Evaluate` had three
manual save/restore pairs around `ibBackendException::SetEvalMode`,
one per early-return / catch path. New `ibEvalModeScope` (nested in
`ibBackendException`) captures the previous flag in its ctor and
restores in its dtor, so an inner throw doesn't leak the flag onto
the session. `Evaluate` now drops all three explicit resets.

**`std::string → wxString` across session / web / guid.** The
session id was `std::string` on `ibSession`, `ibSessionRegistry`,
`ibSessionSnapshot`, `ibConnectRequest::SessionFactory`,
`MakeSessionFactory`, `ibWebSession::m_id` etc — every callsite
that touched it had to bridge through `wxString::FromUTF8(...)` /
`ToUTF8().data()`. Migrated the type all the way through. `ibGuid::str()`
now returns `wxString` directly (the old hex-emitter goes through
`wxString::append` instead of `std::string::push_back`). Map keys
in registry / snapshot / kindBySession / exclusiveBySession all flip
to `wxString`. `ibValueGuid::CompareValueEQ/NE` and `GetString()`
simplify accordingly. Web layer (`ibWebSession::m_id/m_user/m_configName`,
`Login(user, password)`) takes `wxString` directly; SessionManager
in `wfrontend.cpp` keeps `std::string` at the C ABI boundary and
converts once on entry.

To support nlohmann::json, new `frontend/web/jsonAdapter.h` provides
`adl_serializer<wxString>` (UTF-8 to/from `std::string`). All web
ToJSON paths drop their `std::string(s.mb_str(wxConvUTF8))` per-field
conversions and assign `wxString` directly. Same in `wfrontendMenuJSON`
/ `wfrontendCtorsJSON` / `SessionInfoFromSession`.

Net effect — the wxString↔std::string bridging in the session-id
codepath is gone; the only remaining boundary is the C ABI of the
HTTP-server interface in `wfrontend.cpp`.

**Pause / Kick — cooperative cancel.** Two places where the old
"set a flag, hope the script reaches it" pattern needed escalation.

- *Debugger Pause* (`ibDebuggerServer::ibDebuggerServerConnection::RecvCommand`,
  `CommandId_Pause` branch) now calls `pool->CancelSession(sess)` in
  addition to setting `m_bDebugStopLine`. Soft path still works for
  scripts hitting line markers; hard path covers tight loops and
  native blocking calls. Trade-off — Pause becomes "I gave up,
  stop it" rather than pause-and-inspect.
- *Admin Kick* (`ibSessionRegistry::JobCheckSignal`, `ibSessionSignal::Kick`
  branch) calls `m_workerPool->CancelSession(it->second.get())` before
  submitting the Remove request. Lets the running script unwind via
  `ibBackendInterruptException` so OnExit / pool drain runs against
  an idle worker rather than racing a still-executing task.

**Worker pool — log task exceptions.** Previously the worker loop's
`catch (...)` just stuffed the exception into the promise. For
`RunOnWorker` callers that's fine — they consume the future and
re-raise. For `PostWork` (fire-and-forget) callers — timer ticks,
session destroy callbacks — the future was discarded and the
exception silently swallowed. Added `LogWorkerException(location)`
helper in `workerPoolHeadless.cpp` that does `try { throw; }` to
identify the type and emits `wxLogWarning` with the message;
called from both the reentrant inline branch and the worker-thread
loop. RunOnWorker callers see double-logging; acceptable cost for
catching timer-driven script bugs.

**`daemon/deamon.vcxproj.filters` typo-orphan removed.** The active
project is `daemon.vcxproj` / `daemon.vcxproj.filters`; the
mis-named `deamon.vcxproj.filters` was never referenced. One file
deletion.

## Verified-landed audit (2026-05-02 PM)

Pass over the long-running architectural items that had been
sitting on the deferred list. Verified against the current code,
not against memory snapshots.

- **mm singleton out of metadata** ✅. `ibMetaDataConfigurationFile`
  no longer carries `m_moduleManager` / `GetModuleManager` (verified
  in `metadataConfiguration.h:103` + `:178`). Per-session `m_root`
  reached via `ibSession::GetModuleManager()` (`session.h:225`).
  `appData.cpp:179-180` does `s->GetModuleManager()->AttachRuntime(s)`.
  `RunDatabase` no longer calls CreateMainModule (comment at
  `metadataConfiguration.cpp:201`); `CloseDatabase` symmetric. The
  remaining `m_moduleManager` greps point at unrelated fields
  (per-Report / per-DataProcessor descriptor mm for External objects,
  procUnit↔mm back-pointer, comment strings) — none of them is a
  config-singleton.

- **Common modules → metadata registry** ✅. New `ibModuleStorage`
  class in `metadata.h:28-36` owns `AddCommonModule` /
  `RenameCommonModule` / `RemoveCommonModule` / `GetInitModules`.
  `ibMetaData::GetModuleStorage()` accessor at `metadata.h:141-142`,
  storage field at `:397`. `metaModuleObject.cpp:151-210` calls
  `storage->Add/Rename/Remove(this)` directly — runtime mm no longer
  mediates designer mutations of the common-module list. Runtime
  mm in `CreateMainModule` will iterate `GetInitModules()` to spawn
  per-session `ibValueModuleUnit`s.

- **Designer session + mm via event-driven path** ✅. Previously
  designer used the legacy singleton; now it goes through
  `ibSessionRegistry::OnFirstConnect` (one-shot `metaDataCreate`)
  + `OnAuthenticated` (per-session `BindSessionToThread` →
  `RunDatabase` once → `CompileRoot` → `AttachRuntime` for
  runtime kinds). Designer kind skips `AttachRuntime`
  (compile-only); intellisense lives off `CompileRoot`'s output.
  Wired in `appData.cpp:137,146`; `mainApp.cpp:269` confirms
  *"OnFirstConnect / OnAuthenticated — nothing to compose here"*.

- **Web session singleton bug** ✅ (already closed 2026-04-20,
  re-confirmed). Per-tab `sys_session` rows are inserted via
  `ibSessionRegistry::Connect(req)` + `ticket.Attach(user, pwd)`;
  user info reads through `ibSession::Current()` with singleton
  fallback. Last-login-wins symptom gone.

- **ibConnectionPool API surface** ✅ — actually consolidated; the
  earlier "16 public methods" count was a header mis-read. Public
  surface (header line 54-117): `Init` / `Shutdown` / `IsInitialised`,
  static `GetFreeConnection()` (RAII scope — the borrow path),
  static `GetDatabaseLayer()` (the `db_query` macro target), four
  size getters for diagnostics. Everything else (Checkout / Return /
  Reserve* / Bind* / Get* / FindBoundHolder / ReleaseAll /
  WrapHandout / Reap* / *ActiveTxConnection / CurrentHolder /
  ThreadHolder / GetPrimaryConnection) sits under `private:` line
  119, accessed only via `friend class ibConnectionScope` /
  `ibDatabaseConnectionHolder` / `ibSingleConnectionHolder` /
  `ibDatabaseLayer`. Effective work-doing surface is two:
  `GetFreeConnection` and `GetDatabaseLayer`.

- **ibNumber Round half-mode parity — implementation** ✅ (test
  pending). `ibRoundMode` enum in `system/systemEnum.h:27`
  (`Round15as10` = round-half-down, old ttmath default;
  `Round15as20` = half-away-from-zero, new ibNumber default).
  `ibValueSystemFunction::Round(value, precision, mode)` in
  `systemManagerFunc.cpp:44` dispatches: `Round15as20` calls
  `fNumber.Round(precision)` directly; `Round15as10` adjusts by
  `±0.4 × 10^-precision` based on sign and then rounds — comment
  at line 49-52 explicitly states *"equivalent to old ttmath
  behaviour"*. Script default is `Round15as20`. Open: golden test
  on financial 0.5-копеечные edge cases — implementation present
  but not verified against a recorded ttmath baseline.

- **Designer ↔ Runtime split** ✅. No legacy compile-cache leakage
  between halves. `ibCompileValueCache` on `ibMetaData`
  (`metadata.h:402`) is *designer-only* by construction — allocated
  by `ibMetaDataConfigurationStorage`'s ctor, stays nullptr on
  runtime configurations. Lives on metadata as the *target* state
  (per `project_compile_cache_bringup_2026_04_26`), not as a
  grandfathered field. Designer compile-only mode and runtime full
  mode use the same registry/lifecycle; the split is just a
  `wantsRuntime` flag in `appData.cpp:174-181` that gates
  `AttachRuntime`.

- **Metadata-tree vs runtime-tree** ✅. `ibValueMetaObject` is a
  pure descriptor — no `m_procUnit` / `m_compileModule` /
  `m_byteCode` fields anywhere on it (verified by grep). Runtime
  side lives in `ibRuntimeModuleDataObject` mixin which is
  composed into `ibValueRecordDataObject*` (per-record / per-form
  runtime instances). `ibSession::m_root` =
  `ibValuePtr<ibValueModuleManagerConfiguration>` — runtime tree
  is on session, metadata tree is on `m_commonObject`. Cross-link
  is one-way: runtime mm-nodes hold a raw pointer to their
  descriptor; reverse pointers don't exist.

- **`ibDesignerSession` size** ✅ already minimal — 9 lines, one
  override. Memory note about "future direction: dissolve
  `ibDesignerSession`" should be read as cleanup-aspirational, not
  pending tech debt:

  ```cpp
  class ibDesignerSession : public ibGUISession {
  public:
      using ibGUISession::ibGUISession;
      bool OnCreateSession() override {
          AttachFrame(new ibFrontendDocMDIFrameDesigner);
          return m_frame != nullptr;
      }
  };
  ```

  All earlier worry about Designer-side mm / debug-bridge / state
  fields is gone — moved to metadata / frame-virtuals / shared
  registry already. What's left is a thin frame-factory
  override, parallel in shape to `ibEnterpriseSession` and
  `ibWebClientSession`. Removing it would just push the same
  factory into a `ibConnectRequest::SessionFactory` lambda; same
  9 lines, slightly worse cohesion. Leave alone.

## AOT cache Steps 2-4 landed (2026-05-02 evening)

Continuation past the verified-landed audit — full hot path now end-to-end.
See `docs/next-session-aot.md` for the updated step-by-step status; below
captures only what's new in the working tree this session.

- **`sys_bytecode_cache` table + DAO** — minimal schema
  `(descriptor_id, bytecode_version, bc_blob)`. Column named `bc_blob`
  rather than `blob` to dodge Firebird's parser collision with the
  type keyword. `MigrateTableBytecodeCache` runs additively in
  `CreateAppDataEnv` / `CreateServerAppDataEnv`. New
  `backend/cache/byteCodeCache.{h,cpp}` with
  `Save / Load / Invalidate / InvalidateAll`. PG uses BYTEA, the rest
  use BLOB. `SetParamBlob(int, void*, long)` only — the wxMemoryBuffer
  overload is a no-op base default.

- **`ibRuntimeModuleDataObject::Compile` AOT hook** — three-arm flow:
  cache hit + dep verify → ready; dep drift → invalidate + recompile;
  cache miss → compile + Save. Phase B (binder + Register + PrepareNames
  on bound values) runs unified for all arms. Live pointers AOT skipped
  on serialize are restored in Phase B: `bc.m_parent`, const-pool
  `m_bReadOnly = true`, and `PrepareNames` on every bound value (the
  fresh-compile path runs all of these inside `PrepareModuleData`,
  which the cache-hit path skips).

- **`ibByteCode` registry + `ResolveAndVerifyDependencies`** — process-
  wide `unordered_map<wxString, ibByteCode*>` keyed by descriptor GUID.
  Static `Register / Unregister / Find` on `ibByteCode` itself; locked
  via `std::shared_mutex` (read-mostly). `Compile` calls `Register` on
  every successful path; descriptor dtor calls `Unregister`.
  `ResolveAndVerifyDependencies` walks `m_dependencyIds`, looks each up,
  checks `m_dependencyVersions[i] == dep->m_version`, populates
  `m_dependencies`. Mismatch → caller treats as case-(c) drift.

- **Designer hooks for proactive invalidation** — `OnSaveMetaObject` on
  `ibValueMetaObjectModuleBase` (in Designer mode + `saveConfigFlag`)
  calls `ibByteCodeCache::Invalidate(GetGuid())` so the next runtime
  session compiles the changed module from source. `OnDeleteMetaObject`
  invalidates too — orphan rows are harmless but pointless to keep.

### Firebird driver fix bundled

`firebirdDatabaseLayer::DoRunQuery` and `DoRunQueryWithResults` —
on a per-statement error inside a quickie-TX path (DDL on existing
table, etc.), the old code rolled back through the driver but bypassed
the public `RollBack()` → `m_txDepth` stayed at 1 forever → next
`OnBeforeSaveDatabase` tripped its `IsActiveTransaction()` guard and
silently refused to save. Now routes through public `RollBack()` so the
depth counter and pool TX-pin clear correctly. Other drivers don't use
the quickie pattern.

### Telemetry from smoke test (FB embedded, ~20-module config)

| descriptor              | blob     | load   |
|-------------------------|----------|--------|
| ManagerModule (×12)     | 832 B    | 3-4 ms |
| CommonModule1           | 824 B    | 4 ms   |
| CommonForm1 / 2 / 3     | 1.5-2 KB | 4-7 ms |
| ObjectModule            | 1209 B   | 4 ms   |

Cache-hit path: `compile = 0 µs`, `save = 0 µs` — compiler not invoked,
just SELECT + DeserializeAOT. Cache-miss "first run" cost matches today's
startup (one full compile per module).

## Still open

- AOT Step 5 — automated cold / warm / edit / drift tests against a
  live appData. Manual smoke is green; gtests not yet wired.
- `oes_tests` first run — 11 + 13 + 14 cases written, never executed;
  runtime cases are smoke tests, may need adjustment (e.g.
  `var publicVar export;` syntax not verified, `If/While` emitting
  `OPER_IF` was an assumption).
- `/admin/diag` HTTP endpoint on wes — JSON `{workerPool,
  connectionPool, sessions, debugQueue}`; same fields in designer's
  Active Users dialog.
- `ibWorkerPoolGUI` scaffold — `frontend/session/workerPoolGUI.{h,cpp}`,
  not auto-installed (header comment: *"current code path runs
  inline on the calling thread"*). Either wire as background-pool
  for heavy ops on desktop GUI, or delete as dead code.
- Load-verify — 50+ concurrent browser tabs against wes; watch
  worker pool / connection pool grow-shrink; multi-tab debug
  per-session isolation.
- Web per-tab DropSession audit — assert / counter that `DropSession`
  is hit on every teardown path (force-kill, idle-timeout, manual
  logout).
- `db_query → Current()` runtime split — `db_query` macro still
  resolves through `ibApplicationData::GetDatabaseLayer()` (per-thread
  holder); runtime path should migrate to `Current()->GetDatabaseLayer()`
  (session holder), keeping `db_query` for DDL / service / bootstrap.
- `ibNumber::Round` golden test — record ttmath-baseline outputs on
  financial 0.5-копеечные cases, assert against current
  `Round15as10` emulation.

## Cosmetic / low-priority

- ttmath credit in `frontend/win/dlgs/about.cpp:10` stays — ethical
  attribution to Tomasz Sowa, not a cleanup target. Stale
  ttmath-compat comments in `number.h/cpp/systemManagerFunc.cpp/
  firebirdResultSet.cpp` are a separate item.
- Print preview sub-pixel gaps — needs DirectWrite path in
  `SetFontColorSettings` (explicit `StyleSetFaceName` /
  `StyleSetSize` instead of `StyleSetFont(wxFont&)`).
- `OpenForm` intermittent `[undefined]` render under heavy
  multi-tab load.
